apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: quickstart-webgui
  namespace: nfcu-demo
spec:
  host: quickstart-webgui-nfcu-demo.<hostname>
  port:
    targetPort: webgui-9090
  tls:
    insecureEdgeTerminationPolicy: None
    termination: passthrough
  to:
    kind: Service
    name: quickstart-webgui
    weight: 100
  wildcardPolicy: None

# update host name
